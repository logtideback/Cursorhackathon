# Operational analytics SQL (reference only)

Copy or run statements from `../../analytics/queries/operational_product_metrics.sql`
against a reporting replica if you want dashboards from **application** tables.

Do **not** treat this folder as a numbered migration. Analytics vendor events
remain outside the app database.
